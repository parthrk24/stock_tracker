# Stock Portfolio Tracker

A real-time stock portfolio tracker built with Python, FastAPI, WebSockets, and SQLite.

## Run

cd backend
uvicorn api:app --reload --port 8000

## Structure

backend/ → FastAPI server, producer, consumer, DB
frontend/ → Dashboard HTML
